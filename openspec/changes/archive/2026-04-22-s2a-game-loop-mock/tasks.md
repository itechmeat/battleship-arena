> This file is self-contained. For extended prose, alternatives considered, and full code listings, see the supplementary plan at `docs/superpowers/plans/2026-04-21-s2a-game-loop-mock.md`. TDD is mandatory per `CLAUDE.md`: write failing tests first, verify they fail, implement the minimum code, verify tests pass, then prepare a commit. Agents MUST NOT run `git add`/`git commit`/`git checkout -b` without explicit user permission (`AGENTS.md`).

## 1. Shared contract additions

- [x] 1.1 Add S2a constants to `shared/src/constants.ts`: `RING_CAPACITY = 200`, `SSE_HEARTBEAT_MS = 25_000`, `SCHEMA_ERROR_DNF_THRESHOLD = 5`, `MOCK_TURN_DELAY_MS_DEFAULT = 150`. Keep `CONSECUTIVE_SCHEMA_ERROR_LIMIT` as an alias of `SCHEMA_ERROR_DNF_THRESHOLD`. Unit tests under `shared/tests/constants.test.ts` assert each value.
- [x] 1.2 Add `run_not_found` and `already_aborted` to the `ERROR_CODES` tuple in `shared/src/error-codes.ts`. Unit test asserts both are members of the closed set.
- [x] 1.3 Add types to `shared/src/types.ts`: `CellState` (union of the four state literals), `BoardView` (`{ size: 10; cells: readonly CellState[] }`, 100-cell runtime invariant documented on the producers), `ShotResult`, `RunMeta` (mirrors the `runs` row minus `client_session`, with `outcome: Outcome | null`), `RunShotRow` (mirrors `run_shots` with `result: ShotResult`), `StartRunInput`. Update `shared/src/index.ts` to re-export everything. Type-level tests under `shared/tests/types.test.ts` construct valid values.
- [x] 1.4 Add `shared/src/board-svg.ts` with the pure `renderBoardSvg(view: BoardView): string` function: fixed viewBox `0 0 640 640`, 64px cells, closed-geometry cell markers, no `<text>` elements. Write a snapshot test at `shared/tests/board-svg.test.ts` that asserts byte-equality against a committed fixture at `shared/tests/fixtures/board-svg/mixed.svg` (bless on first run via a one-liner `bun -e` then commit the fixture).
- [x] 1.5 Add `shared/src/sse-events.ts` with the `SseEvent` discriminated union (variants `open`, `shot`, `resync`, `outcome`; every variant has numeric `id`) and `isSseEvent(value): value is SseEvent` runtime guard. Unit test asserts the guard accepts well-shaped events and rejects unknown kinds or missing id.

## 2. Board module (backend)

- [x] 2.1 Install `@resvg/resvg-js` in the backend workspace only (`cd backend && bun add @resvg/resvg-js`). Verify via `bun -e 'import { Resvg } from "@resvg/resvg-js"; console.log(new Resvg("<svg xmlns=\"http://www.w3.org/2000/svg\" width=\"1\" height=\"1\"/>").render().asPng().length)'`. Re-run `bun install --frozen-lockfile` and confirm no lockfile changes.
- [x] 2.2 Implement `backend/src/board/generator.ts` exporting `generateBoard(seedDate: string): BoardLayout`. Use `xoshiro128**` seeded from the first 16 bytes of `SHA-256(seedDate + ':' + salt)` reduced to four u32 words. Place each ship via rejection sampling (random orientation, random anchor, check bounds, no overlap, no 8-neighborhood adjacency with already-placed cells); on too many attempts restart the full layout with the next salt (max 64 restarts before throwing). Tests at `backend/tests/unit/board-generator.test.ts`: fixed-seed determinism, fleet composition matches shared `FLEET`, total 17 cells, 50-seed property-based test for in-range / no-overlap / no-adjacency / no-diagonal-touch, round-trip of `seedDate`.
- [x] 2.3 Implement `backend/src/board/renderer.ts` exporting `renderBoardPng(view: BoardView): Uint8Array` = `new Resvg(renderBoardSvg(view)).render().asPng()`. Tests at `backend/tests/unit/board-renderer.test.ts`: PNG signature (`0x89 0x50 0x4E 0x47`), byte-identical for identical input, byte-equal snapshot against a committed fixture at `backend/tests/fixtures/board-png/all-unknown.png` (bless on first run).

## 3. Mock provider (backend)

- [x] 3.1 Implement `backend/src/providers/types.ts` with `ProviderAdapter`, `ProviderModel`, `ProviderCallInput` (including the new `seedDate: string` field, which extends `docs/spec.md` 6.1), `ProviderCallOutput`, and `createProviderRegistry(adapters)` returning `{ get, listIds }`. Tests at `backend/tests/unit/provider-registry.test.ts`: `get` returns the registered adapter; `get` returns `undefined` for unknown id; `listIds` enumerates.
- [x] 3.2 Implement `backend/src/providers/mock.ts` with `createMockProvider({ delayMs?: number })` returning an adapter with `id = "mock"` and three models (`mock-happy`, `mock-misses`, `mock-schema-errors`). Use an `AbortSignal`-aware `sleep(ms, signal)` helper that clears its timer on abort. Variant logic:
  - `mock-happy`: hunt (stride-2 checkerboard) + target (four orthogonal neighbors of the last hit in `priorShots`), always returns `JSON.stringify({row, col})` with in-range non-duplicate coords.
  - `mock-misses`: `generateBoard(input.seedDate)` to derive ship cells, iterate non-ship cells in row-major order, return the first non-ship cell not in `priorShots`. Fallback after 83 non-ship cells exhausted: re-fire `priorShots[0]` (duplicate-shot -> `invalid_coordinate`), NOT out-of-range.
  - `mock-schema-errors`: return a single constant malformed payload every call (for example the literal `"not json"`). Do NOT key selection by `priorShots.length`, because in a schema-error-only run the filtered `priorShots` array (hit/miss/sunk only) stays empty and a length-keyed cycle would never advance. A constant payload is sufficient: the engine's DNF trigger counts consecutive `schema_error` results regardless of payload content.
    Tests at `backend/tests/unit/providers-mock.test.ts`: mock-happy wins a full simulated game in <= 100 shots; mock-misses never collides with ship cells until exhausted then falls back to `priorShots[0]`; mock-schema-errors always returns a payload whose `parseShot` result is `kind: "schema_error"` (including with `priorShots: []`); unknown modelId rejects; abort during sleep rejects promptly with `AbortError`.

## 4. Run lifecycle (backend)

- [x] 4.1 Implement `backend/src/runs/outcome.ts` exporting `reduceOutcome(state, event)` as a pure `{ state, outcome: Outcome | null }` reducer plus `initialRunLoopState()`. State fields: `shotsFired`, `hits`, `consecutiveSchemaErrors`, `schemaErrors`, `invalidCoordinates`. Event kinds: `hit`, `miss`, `sunk`, `schema_error`, `invalid_coordinate`, `abort {reason: "viewer" | "server_restart"}`. Transitions per `docs/spec.md` 3.5 + 4.2 exactly. Tests at `backend/tests/unit/runs-outcome.test.ts` cover every transition and reset rule.
- [x] 4.2 Implement `backend/src/runs/event-ring.ts` exporting `class EventRing` with `push(event)` (assigns monotonic `id` starting at 1, drops oldest on overflow) and `since(lastEventId: number | null): SseEvent[] | "out_of_range"`. Tests at `backend/tests/unit/event-ring.test.ts`: ids start at 1 and are monotonic, overflow drops oldest, `since(n)` returns `id > n`, `since(old_n)` returns `"out_of_range"`.
- [x] 4.3 Implement `backend/src/db/queries.ts` exporting `createQueries(db)` returning `Queries` with: `insertRun(args)`, `appendShot(args)`, `finalizeRun(args)`, `getRunMeta(id)`, `listShots(runId)`, `findStuckRunIds()`, `markStuckRunsAborted(outcome, endedAt)`. Integration tests at `backend/tests/integration/queries.test.ts` use `withTempDatabase` to round-trip every method.
- [x] 4.4 Implement `backend/src/runs/reconcile.ts` exporting `reconcileStuckRuns(queries, nowMs)` that delegates to `queries.markStuckRunsAborted("aborted_server_restart", nowMs)`. Integration test at `backend/tests/integration/reconcile.test.ts`: seeds a row with `outcome=NULL, ended_at=NULL`, calls the function, asserts the row is updated; also verifies `0` is returned when there are no stuck rows.
- [x] 4.5 Implement `backend/src/runs/engine.ts` exporting `runEngine(runId, input, signal, emit, deps)`. Loop per turn: compute `BoardView` from prior shots + layout, render PNG, compute ships-remaining preamble, call adapter with `seedDate` and filtered `priorShots` (hit/miss/sunk only), parse via `parseShot`, classify, persist `run_shots` row (raw truncated to 8 KiB, reasoning to 2 KiB), emit shot event, apply reducer, exit on terminal. Honor `AbortSignal`: on AbortError read `signal.reason?.reason` ("viewer" vs "server_restart"); when reason is `"server_restart"` exit without writing terminal outcome (reconcile finalizes on next boot). Emit `open` event first and final `outcome` event for all non-server-restart terminals. Hold `input.apiKey` only in the function's closure; never copy to any outer scope. Integration tests at `backend/tests/integration/engine.test.ts`: mock-happy reaches `won`; mock-misses reaches `dnf_shot_cap` with `shotsFired=100`, `hits=0`, `schemaErrors=0`, `invalidCoordinates=17`, and exactly 83 `miss` + 17 `invalid_coordinate` rows; mock-schema-errors reaches `dnf_schema_errors` with exactly 5 schema_error rows; mid-run abort with reason `"viewer"` reaches `aborted_viewer`; API key never appears in any column of `runs` or `run_shots` (scan `SELECT *` output for the sentinel substring).
- [x] 4.6 Implement `backend/src/runs/manager.ts` exporting `createManager(deps): Manager` with `start(input)`, `abort(runId, reason)`, `getHandle(runId)`, `shutdown(graceMs)`. Use `generateUlid` from the existing `backend/src/db/ulid.ts` (do NOT install a new `ulid` package). Each `RunHandle` carries an `AbortController`, an `EventRing(RING_CAPACITY)`, a `Set` of subscriber callbacks, and a `taskPromise` that resolves when the engine returns. Manager MUST have no field that can hold an apiKey; `input.apiKey` passes through to `runEngine` only. Integration tests at `backend/tests/integration/manager.test.ts`: start + await task reaches `won`; subscribe delivers live events including the final `outcome`; mid-run abort produces `aborted_viewer`; `shutdown(0)` while a slow run is in flight aborts with reason `"server_restart"`, the engine exits without writing terminal, and a subsequent `reconcileStuckRuns(queries, now)` finalizes the row to `aborted_server_restart`.
- [x] 4.7 Add `backend/tests/integration/manager-key-reachability.test.ts` as a regression canary for the in-memory arm of the key-never-persists guarantee. Submit a distinctive sentinel `apiKey` to `manager.start(...)`, then, both while the run is in flight and after it terminates, assert the sentinel substring is NOT present in any of: `JSON.stringify(manager)`, `JSON.stringify(manager.getHandle(runId))`, the concatenation of `JSON.stringify(event)` over `handle.ring.since(null)`, or the `JSON.stringify` of every subscriber function's registered state accessible via iteration of `handle.subscribers`. Document inside the test file that this is a regression canary for enumerable state - it does not cover non-enumerable properties, symbol-keyed fields, `WeakMap` entries, or closure-captured values; the structural guarantee (Manager's TypeScript types do not declare a field or retained parameter of `apiKey` type) is the primary defense, enforced by code review at PR time. Compile-time check complement: optionally add a `tsc --noEmit` assertion by writing `backend/tests/unit/manager-type-shape.test-d.ts` (or a commented snippet) that would fail type-checking if `Manager` or `RunHandle` gained an `apiKey`-typed field.

## 5. Runs API (backend)

- [x] 5.1 Implement `backend/src/api/session.ts` with `sessionMiddleware` (reads `bsa_session` cookie; if absent generates a new ULID via `generateUlid` and sets it on the response with `HttpOnly; Secure; SameSite=Strict; Path=/; Max-Age=31536000`) and `readSession(c): string`. Integration test at `backend/tests/integration/session.test.ts`: absent cookie triggers `Set-Cookie`; present cookie is read through without rotation.
- [x] 5.2 Implement `backend/src/api/runs.ts` exporting `createRunsRouter({ manager, queries, providers })`. First handler: `POST /runs` with hand-rolled JSON validation (no validator dep). Validation: `providerId` non-empty string and in registry; `modelId` non-empty string and in adapter's `models`; `apiKey` non-empty string; optional `budgetUsd` is a positive finite number. Any failure returns 400 `invalid_input` with `detail.field`. On success set `Cache-Control: no-store`, read session via `readSession(c)`, compute `seedDate = new Date().toISOString().slice(0, 10)`, call `manager.start({ providerId, modelId, apiKey, budgetUsd, clientSession, seedDate })`, return `{ runId }`. MUST NOT log or echo `apiKey`. Tests at `backend/tests/integration/api-runs-post.test.ts`: happy path; empty apiKey rejected; unknown modelId rejected with `detail.field === "modelId"`; negative budget rejected; response contains `Cache-Control: no-store` and no submitted apiKey substring.
- [x] 5.3 Extend the runs router with `GET /runs/:id`, `GET /runs/:id/shots`, `POST /runs/:id/abort`. `GET :id` returns the `RunMeta` row (or 404 `run_not_found`); `GET :id/shots` returns `{ runId, shots }` ordered by idx (or 404); `POST :id/abort` on a terminal run returns 200 with `{ outcome }` (NOT 409) and on an active run calls `manager.abort(id, "viewer")`. Tests at `backend/tests/integration/api-runs-get.test.ts` cover each endpoint including terminal-abort idempotency and unknown-id 404 paths.
- [x] 5.4 Extend the runs router with `GET /runs/:id/events` SSE handler using Hono's `streamSSE` from `hono/streaming`. Resolve `Last-Event-ID` from the header of that name OR the `lastEventId` query param (header takes precedence). Three code paths:
  - Active run, backlog within horizon: replay backlog ring events, subscribe for live events, heartbeat every `SSE_HEARTBEAT_MS` via `stream.write(": heartbeat\n\n")`.
  - Active run, backlog out of horizon: emit one `resync` event and close.
  - No active handle: if `getRunMeta(id)` returns a terminal outcome, synthesize full replay (`open` event with run info + one `shot` event per persisted row in idx order + `outcome` event built from the meta row) with ids re-assigned 0, 1, 2, ... and close; otherwise emit one `resync` event. Tests at `backend/tests/integration/api-runs-events.test.ts`: active mock-happy run streams `open + shot + ... + outcome`; terminal run streams the full synthesized replay with shot count matching persisted rows and no `resync`; unknown id streams exactly one `resync`.
- [x] 5.5 Wire `backend/src/app.ts` and `backend/src/index.ts`:
  - `createApp(metadata, runs)` mounts `sessionMiddleware` globally, then `createHealthRouter(metadata)` and `createRunsRouter(runs)` under `/api`.
  - `bootstrap(config)` opens the database, creates `Queries`, calls `reconcileStuckRuns(queries, Date.now())`, builds the mock registry via `createProviderRegistry({ mock: createMockProvider({ delayMs: config.mockTurnDelayMs }) })`, builds the manager with `{ db: queries, providers, generate: generateBoard, renderBoard: renderBoardPng, now: () => Date.now() }`, constructs the app, starts `Bun.serve`, registers `SIGTERM` and `SIGINT` handlers that trigger a shared shutdown path, log shutdown failures, and attempt `server.stop()` in `finally`, and returns `{ server, sqlite, manager, config }`.
  - Extend `backend/src/config.ts` with `mockTurnDelayMs` parsed from `MOCK_TURN_DELAY_MS` (non-negative integer, default 150).
  - Integration smoke test at `backend/tests/integration/app-wiring.test.ts`: `/api/health` returns 200 and `POST /api/runs` returns 200 in the same app instance.

## 6. Web layer

- [x] 6.1 Implement `web/src/lib/api.ts` with `ApiError` class wrapping `ErrorEnvelope + status`, a `request<T>` helper that decodes `ErrorEnvelope` on non-2xx and throws `ApiError`, and typed functions `startRun`, `getRun`, `getRunShots`, `abortRun` using `credentials: "same-origin"` so the session cookie rides along. Implement `web/src/lib/sse.ts` with `subscribeToRun(runId, { lastEventId, onEvent, onResync, onError }): () => void` that opens `EventSource('/api/runs/<id>/events?lastEventId=<n>')`, registers separate listeners for `open`, `shot`, `outcome`, `resync`, re-opens on `EventSource.readyState === CLOSED`. Return a close function.
- [x] 6.2 Implement `web/src/islands/boardViewFromShots.ts` as the pure helper: iterate shots, place `miss` / `hit` / `sunk` results at index `row * 10 + col`, then for every `sunk` shot flood outward along row and column and upgrade adjacent `hit` cells to `sunk` until a non-hit/sunk cell is reached. Null-coord shots (schema errors) are skipped. Unit test at `web/tests/unit/boardViewFromShots.test.ts` covers: empty shots -> all unknown; miss + hit placed at row-major position; sunk upgrades contiguous hits along the row; null coordinates skipped.
- [x] 6.3 Implement `web/src/islands/BoardView.tsx` as a Solid component: memoize `renderBoardSvg(boardViewFromShots(props.shots))` and inject via `innerHTML` on a div with `width: 100%; max-width: 480px; aspect-ratio: 1 / 1; margin: 0 auto`.
- [x] 6.4 Implement `web/src/islands/StartRunForm.tsx` with Solid signals for `providerId` ("mock"), `modelId` (default "mock-happy"), `apiKey`, `budgetUsd`, `busy`, `error`. Submit: call `startRun`, on success `location.assign('/runs/' + runId)`; on `ApiError` render `error.envelope.error.message` inline; set `aria-busy` and `disabled` during inflight. Provider select has one option ("mock"); model select has three options (`mock-happy`, `mock-misses`, `mock-schema-errors`) with human-friendly labels. Add `web/src/styles/play.module.css` with a mobile-first single column, 44px min-height controls, centered up to 480px.
- [x] 6.5 Implement `web/src/islands/LiveGame.tsx` with the four-phase state machine (`loading`, `live`, `terminal`, `error`). On mount: `getRun(props.runId)` -> if terminal, fetch `getRunShots` and transition to terminal; else fetch shots, compute `lastEventId` from `shots[-1]?.idx ?? null`, open `subscribeToRun`. `onEvent` for `shot` appends to shots; for `outcome` updates meta and transitions to terminal. `onResync` closes and re-runs the mount sequence. Render `<BoardView shots={shots()} />` plus a HUD (`shotsFired`, `hits`, `schemaErrors`, `invalidCoordinates`, `outcome` when terminal), plus an Abort button while `phase === "live"` that calls `abortRun`. Add `web/src/styles/live-game.module.css` with the same mobile-first constraints.
- [x] 6.6 Add Astro pages:
  - `web/src/pages/play.astro`: shell with `<StartRunForm client:load />`.
  - `web/src/pages/runs/[id].astro`: reads `Astro.params.id`, redirects to `/play` if missing, renders `<LiveGame runId={id} client:load />`.

## 7. Verification

- [x] 7.1 From the repo root, run `bun install --frozen-lockfile`, `bun run lint`, `bun run fmt:check`, `bun run typecheck`, `bun run test`, `bun run build`. Every command MUST exit 0.
- [ ] 7.2 Local-dev walkthrough:
  - Terminal A: `cd backend && DATABASE_PATH=/tmp/bsa-dev.db MOCK_TURN_DELAY_MS=150 bun run dev`.
  - Terminal B: `cd web && bun run dev`.
  - In a browser, submit `/play` with each of the three mock models. Confirm `mock-happy` transitions to `won`, `mock-misses` to `dnf_shot_cap`, `mock-schema-errors` to `dnf_schema_errors`, all without page reload.
  - Reload the `/runs/:id` page during a mock-happy run and confirm the live view resumes from the ring and continues receiving events.
- [ ] 7.3 Key-never-persists sanity: `sqlite3 /tmp/bsa-dev.db "SELECT * FROM runs; SELECT * FROM run_shots LIMIT 5;"` and confirm no column contains any submitted API key substring.
- [x] 7.4 Update `docs/spec.md` section 6.1 to add `seedDate: string` to `ProviderCallInput` in the same PR that introduces the code change, so the spec and the type cannot drift.
- [ ] 7.5 Prepare a PR description that lists the shipped capabilities, the three-outcome walkthrough, and the deferred items (Playwright to S2b, real providers to S3). Do NOT push or open the PR without user permission per the Git-command policy.
